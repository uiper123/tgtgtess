# Changelog

Все значимые изменения проекта документируются здесь.
Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/),
проект использует [семантическое версионирование](https://semver.org/lang/ru/).

## [Unreleased]

_Нет несвязанных с релизами изменений._

## [1.5.9] — 2026-08-30

### 🎯 Accuracy & Score Calculation Fixes
- **Интеллектуальный поиск оригинального теста и точный подсчёт баллов при импорте логов:** При импорте резервных `.log` файлов внедрен алгоритм `_find_test_file_in_repository`, который сопоставляет лог с эталонными тестами репозитория по имени файла, названию группы, заголовку, а также сигнатуре текста вопросов (fingerprint). Это позволяет автоматически подгрузить эталонные правильные ответы, рассчитать абсолютно точную оценку и отобразить верные/неверные бейджи и правильные варианты в детальном просмотре ответов студента.
- **Устранение ложных 100% оценок:** Устранена ошибка, при которой при отсутствии эталонного теста оценка по умолчанию приравнивалась к общему количеству ответов (`len(answers)/len(answers)`). Теперь при необходимости сервер предлагает преподавателю выбрать тест из репозитория.

## [1.5.8] — 2026-08-30

### 🐛 Bug fixes
- **Исправлена ошибка просмотра ответов импортированных логов (`'str' object has no attribute 'get'`):** Диалог просмотра ответов студента (`StudentAnswersDialog`) теперь корректно и безопасно обрабатывает как полные вопросы с сервера с флагами `correct: True/False`, так и вопросы из резервных снимков лога (`.log`), где варианты ответов представлены строками. Оценка, выбранные ответы и правильные варианты отображаются без сбоев.

## [1.5.7] — 2026-08-30

### 📁 Directory Chooser Improvements
- **Полноценная видимость всех каталогов системы:** В режиме выбора папки (`DirectoryChooserDialog` / `CHOOSE_DIR`) внедрена динамическая индексация текущей директории через `setRootPath(current_dir)` и установка корневого индекса дерева `setRootIndex`, благодаря чему при открытии любой папки (например, `/home/danil`) отображаются сразу абсолютно все существующие подкаталоги («Рабочий стол», «Документы», «Загрузки», «Тесты» и др.) без пропусков или зависаний отложенной загрузки Qt.
- **Интуитивная навигация по каталогам:** Двойной клик на папку мгновенно открывает её содержимое, кнопка «Вверх ⬆» и быстрый доступ позволяют быстро перемещаться по дереву файловой системы.

## [1.5.6] — 2026-08-30

### 🐛 Bug fixes & UI Polish
- **Исправление открытия файлов из вложенных папок:** Исправлена ошибка, из-за которой при навигации и выборе файла в развернутом подкаталоге дерева (например, `Рабочий стол / Резервная копия / Бэкап_*.log`) путь формировался относительно корня окна вместо родительской папки файла, вызывая ошибку «Файл не найден». Теперь текущий каталог и абсолютный путь файла синхронизируются мгновенно.
- **Исправление масштаба и вертикального клиппинга заголовков столбцов:** Зафиксирована высота заголовка (`36px`), настроено вертикальное центрирование текста и нулевые вертикальные отступы `QHeaderView::section`, устранено срезание нижней части букв на экранах с масштабированием DPI.

## [1.5.5] — 2026-08-30

### 📁 File Manager & UI Improvements
- **Отображение всех каталогов системы при фильтрации файлов:** В файловом менеджере `StyledFileDialog` включен флаг `QDir.AllDirs`, благодаря чему при активном расширении файлов (например, `*.xlsx`, `*.log`, `*.txt`) папки не отфильтровываются и всегда доступны для навигации. Добавлены системные точки монтирования и диски в панель быстрого доступа.
- **Локализация и исправление масштаба столбцов файлового менеджера:** Внедрена модель `CustomFileSystemModel` с русскими заголовками («Имя файла / папки», «Размер», «Тип», «Дата изменения»). Исправлены пропорции и стили заголовков столбцов (убран принудительный ALL CAPS, оптимизированы отступы, включено пропорциональное растяжение имени файла).

## [1.5.4] — 2026-08-30

### 🎨 UI & UX
- **Чёткое отображение файлов и каталогов при выборе/импорте тестов из репозитория:** В диалоге выбора сохранённых тестов (`SelectTestFromRepoDialog`) добавлена отдельная колонка «Файл теста» (имя файла в каталоге) и «Заголовок теста» (метаданные `@title`). Теперь тесты с одинаковыми заголовками легко отличить по имени файла и папке, точно так же как в репозитории на дашборде. Поиск расширен на имена файлов, каталоги и заголовки.

## [1.5.3] — 2026-08-30

### 🐛 Bug fixes
- **Исправление вызова файлового менеджера при экспорте и импорте:** Исправлен относительный импорт `StyledFileDialog` в `server/ui_server.py`, из-за которого в скомпилированном автономном бинарнике при нажатии на кнопки «Экспортировать в Excel» и «Импортировать лог студента» возникал сбой `attempted relative import with no known parent package` и диалоговое окно не появлялось.

## [1.5.2] — 2026-08-30

### 💾 Self-Contained Backups & Smart Import
- **Хранение полной структуры теста в логах резервной копии:** В бэкапы студента (`.log`) теперь упаковывается полный снапшот сессии (вопросы `questions`, варианты ответов, тип заданий, заголовок, раздел, длительность, ответы и подтверждённая сервером оценка).
- **Мгновенный импорт в 1 клик:** На сервере устранена необходимость вручную искать файл теста (`.txt`/`.json`) при импорте лога студента — результат рассчитывается и загружается моментально без всплывающих диалоговых окон.
- **Автономный просмотр результатов:** Двойной клик по строке студента в таблице результатов сразу открывает детальный разбор вопросов и ответов из сохранённого снапшота, даже если исходный файл теста был перемещён или удалён.

### 🎨 Unified File Manager (StyledFileDialog)
- **Единый файловый менеджер в дизайне приложения:** Разработан встроенный двухпанельный диалог `StyledFileDialog` с панелью быстрого доступа (репозиторий тестов, рабочий стол, загрузки, диски), навигацией по каталогам, просмотром метаданных файлов, фильтрацией типов и созданием папок.
- **Полная интеграция:** Все операции открытия и сохранения файлов (экспорт/импорт тестов, ведомостей Excel/CSV, изображений, логов) переведены на единый визуальный стиль.

### 🧪 Tests & Quality
- Набор тестов расширен до **101 теста** (добавлены проверки rich-бэкапов и режимов файлового менеджера).

## [1.5.1] — 2026-08-30

### 🐛 Bug fixes & Security
- **Исправление сборки готовых бинарников в CI/CD (GitHub Actions):** Добавлена полная установка `requirements-dev.txt` перед запуском сборщика Nuitka. Исправлена ошибка, из-за которой в скомпилированные релизы не попадал пакет `cryptography` (приводя к сбою `Не удалось подписать обновление: Пакет cryptography не установлен` и отказу клиентов ставить неподписанные обновления).
- **Включение пакетов верификации и экспорта:** В автономные сборки включены пакеты `certifi` (надежная проверка TLS-сертификатов GitHub API) и `openpyxl` (экспорт результатов в Excel).
- **Поддержка PyInstaller и Nuitka:** В [`build_all.py`](build_all.py) добавлена поддержка двух сборщиков (`--builder=auto|nuitka|pyinstaller`) с автоматическим включением всех необходимых ресурсов (`shared/update_public_key.pem`, `shared/icons`, `image.ico`, `image.png`).

## [1.4.8] — 2026-08-28

### 🛡️ Security & Kiosk
- **Безопасная система внутренних оверлеев (In-App Modal Overlays).** Все модальные диалоги студента (подтверждение завершения теста, предупреждения о попытке списать, блокировка за нарушения, остановка экзаменатором) переведены на встроенный полноэкранный оверлей `InAppModalOverlay`. Полностью устранены нативные окна ОС с заголовками и кнопками сворачивания, закрыта уязвимость открытия меню «Пуск» и переключения рабочих столов во время предупреждений.
- **Подтверждение завершения тестирования.** Добавлено модальное окно перед окончательной отправкой результатов с выводом количества отвеченных и пропущенных вопросов.

### 🎨 UI & UX
- **Иерархическое разделение каталогов и тестов.** В диалоге выбора теста из репозитория («Каталог», «Название теста», «Вопросов») и на карточке запуска экзамена каталог и название теста четко разделены.
- **Очистка навигационной панели студента.** Удален лишний текст статуса сохранения внизу экрана во время тестирования (сохранение продолжает надежно работать в фоне).

### 🧪 Tests & Quality
- Набор тестов расширен до **82 тестов** (проверена поддержка вложенных папок тестов и сценариев поиска).

## [1.4.7] — 2026-08-28

### ✨ Features
- **UDP Авто-обнаружение серверов в локальной сети (Zero-Config Connection).** Сервер рассылает широковещательные маяки (`DISCOVERY_PORT = 9877`), а клиент студента мгновенно обнаруживает активный сервер экзаменатора без необходимости ручного ввода IP-адреса.
- **Встроенный визуальный редактор тестов (GUI Constructor).** Добавлен полнофункциональный редактор тестов на стороне сервера: добавление/редактирование/удаление вопросов, поддержка всех типов (одиночный, множественный, соответствие, порядок, пропуски) и сохранение в каноничный текстовый формат `.txt`.
- **Профессиональный экспорт отчётов в Excel (.xlsx).** Реализован стилизованный экспорт результатов через `openpyxl` с цветовым выделением оценок, красивыми шапками, автоподбором ширины колонок и сводной статистикой.
- **Настраиваемая директория репозитория тестов.** В настройках сервера добавлена возможность выбора произвольной папки для хранения базы тестов с автоматической синхронизацией и обновлением UI.
- **Авто-бэкап результатов на Рабочий стол студента.** При завершении тестирования копия результатов автоматически сохраняется на Desktop студента (`~/Desktop`), обеспечивая сохранность данных при любых сетевых неполадках.

### 🧪 Tests & Quality
- Набор тестов расширен: добавлены тесты UDP discovery, генерации Excel-отчетов, сериализации/парсинга .txt и управления хранилищем.

## [1.4.2] — 2026-05-29

### 🐛 Bug fixes
- **Сбой проверки обновлений (SSL: CERTIFICATE_VERIFY_FAILED).** Добавлен надежный механизм обхода ошибок верификации сертификатов при работе с GitHub API (особенно критично в учебных и корпоративных сетях с SSL-инспекцией). Вся безопасность при этом гарантирована за счет обязательной проверки Ed25519-подписи скачиваемых бинарников.
- **Статический импорт certifi.** Добавлен явный импорт certifi на верхний уровень серверного приложения, гарантирующий корректное включение пакета корневых сертификатов при сборке бинарного файла через Nuitka.

## [1.4.1] — 2026-05-29

### 🐛 Bug fixes
- **Сбой подключения студента.** Исправлена критическая ошибка PySide6 `TypeError` при переходе к тестированию из-за расхождения количества аргументов сигнала `connected_ok` (ожидалось 7, передавалось 6) и неверной сигнатуры слота.
- **Окно режима киоска (fullscreen) на Linux/Windows.** Исправлена проблема, при которой окно клиента при переходе к тестированию оставалось в обычном маленьком окне в центре экрана. Добавлен флаг `Qt.FramelessWindowHint`, окно скрывается перед сменой флагов, и его геометрия принудительно устанавливается равной геометрии основного монитора.
- **Ошибка `AttributeError` при нарушении прокторинга.** Добавлена корректная инициализация `self._cheat_warning_limit` в клиенте.
- **Сбой записи результатов на сервере.** Исправлен вылет `ValueError: I/O operation on closed file` при завершении теста из-за некорректных отступов при атомарном сохранении `results.json` на диске.

## [1.4.0] — 2026-05-28

Большой релиз: подписанные обновления, защита от читерства через
переподключение, чистка UI, 63 теста, и пачка мелких фиксов.

### 🔐 Security
- **Ed25519-подпись авто-обновлений.** Клиент v1.4.0+ отказывается
  запускать `.new` без валидной подписи. Закрывает RCE через фейковый
  сервер в той же LAN.
- **Якорь экзаменационного таймера.** Время измеряется от `exam_start_time`
  в `attempts`, а не от `connect_time` сокета — студент больше не может
  получить лишнее время, выдернув Wi-Fi и переподключившись.
- Атомарная запись `results.json` (через `tempfile + os.replace + fsync`):
  крах процесса больше не теряет всю историю.
- Path-traversal в `@image:` парсера тестов закрыт (`@image:../../etc/passwd`
  отвергается).
- TLS-запросы к GitHub API больше не отключают проверку сертификата
  (используется `certifi`).
- `MAX_MESSAGE_SIZE` снижен с 500 МБ до 64 МБ — закрывает DoS-вектор.
- Дебаунс focus-loss событий (2 сек): системные всплывашки больше не
  считаются за 3 нарушения подряд.

### ✨ Features
- **Конфигурируемый лимит нарушений прокторинга** — отдельный спин-бокс
  «Нарушений» на странице запуска экзамена (1–20, по умолчанию 3).
- Полный обзор системы обновлений: единый метод `send_update_to_socket`
  с подписью, sidecar-метаданные (`.new.sha256` + `.new.sig`) для повторной
  проверки перед запуском, проверки места на диске и прав на запись.
- Версия теперь видна везде в UI: заголовки окон, плашка в сайдбаре
  сервера, футер логин-экрана клиента, диалог обновления.

### 🐛 Bug fixes
- **Сломанный импорт** `get_scaled_qss` в `ui_dialogs.py` и `ui_server.py`
  (искали в локальном `styles.py`, а функция в `shared/styles.py`).
- Кнопка «Подключиться к тестированию» обрезалась на любом размере —
  карточка имела `setFixedWidth(420)`. Теперь адаптивно `min 380, max 520`.
- Карточки login и результата плавали наверху больших экранов вместо
  центрирования — добавлены `addStretch` сверху/снизу.
- Стрелки QComboBox («Все статусы», «Сортировка», «Группа») вообще
  не рендерились (`data:image/svg+xml;base64` URI Qt не загружает).
  Перешли на реальные SVG в `shared/icons/` через хелпер
  `inject_icon_paths()`.
- Колонка «Набранные баллы» в таблице результатов резалась до
  «абранные балл» (130 → 160 px).
- Цвет «Сервер: Выключен» в сайдбаре — был почти не читаем (`#64748b`
  на тёмно-синем). Поднят до `#cbd5e1`.
- Updater клиента: `update.bat`/`.sh` теперь пишется в `%TEMP%` через
  `tempfile.mkstemp`, а не в текущей рабочей директории (которая может
  быть `C:\Windows\System32` при запуске из меню Пуск).
- Updater корректно работает с путями содержащими пробелы (`Program Files`).
- `update_apply` без `.new` файла теперь логирует понятную ошибку
  вместо тихого падения.
- Минимальный размер окна сервера снижен с 1200×750 до 980×640 —
  помещается на старых ноутбуках 1024×768.

### 🧪 Tests + CI
- 63 теста: парсер, скоринг, безопасность (Ed25519 sign/verify), полный
  end-to-end через реальные TCP-сокеты, сценарии миграции, регрессионный
  тест на R-1 (читерство переподключением).
- Новый CI workflow на Python 3.10 / 3.11 / 3.12: ruff + pytest + coverage.

### 🧹 Repo hygiene
- LICENSE (MIT), `pyproject.toml`, pinned `requirements*.txt`,
  `SECURITY.md`, `CONTRIBUTING.md`, `CHANGELOG.md`.
- Удалены случайно закоммиченные `promt.txt`, `test_edited.txt` (5 МБ),
  `фыафыафы.txt`.
- Скрипты для подписи: `scripts/generate_signing_keys.py`,
  `scripts/sign_update.py`.

### 🚚 Миграция со старых клиентов
**Никаких ручных действий не требуется** — клиенты v1.3.6 и старше
не имеют проверки подписи в коде, поэтому при первом подключении к
v1.4.0-серверу автоматически получают и применяют обновление. Они станут
v1.4.0 с встроенным публичным ключом, после чего начнут требовать подпись
для всех будущих обновлений. Подробности — в [SECURITY.md](SECURITY.md).


### 🔐 Security
- **Подпись авто-обновлений (Ed25519).** Сервер подписывает каждый рассылаемый бинарник; клиент отказывается запускать обновление с отсутствующей или невалидной подписью. См. `shared/security.py`, `scripts/sign_update.py`, `scripts/generate_signing_keys.py`.
- **Path traversal в `@image:` запрещён.** Парсер теперь нормализует путь и не выходит за пределы директории с тестом.
- **`MAX_MESSAGE_SIZE` снижен** с 500 МБ до 64 МБ — закрывает простой DoS на стороне сервера и клиента.
- **Verified TLS.** Обращение к GitHub API больше не использует `ssl._create_unverified_context()`. Корневые сертификаты берутся из пакета `certifi`.

### ✨ Added
- Unit-тесты на `shared/parser.py` и `calculate_score` (`tests/`).
- CI-workflow `.github/workflows/ci.yml` — `ruff` + `pytest` на каждый PR.
- `pyproject.toml` с настройками `pytest` и `ruff`.
- `LICENSE` (MIT), `SECURITY.md`, `CONTRIBUTING.md`, `CHANGELOG.md`.
- `requirements.txt` и `requirements-dev.txt` с pinned-зависимостями.

### 🧹 Changed
- Из репозитория удалены случайно закоммиченные временные файлы (`promt.txt`, `test_edited.txt` 5 МБ, `фыафыафы.txt`).

## [1.3.7] — 2025-05

Базовая версия до начала аудита. История релизов: <https://github.com/uiper123/tgtgtess/releases>.
