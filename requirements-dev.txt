# Development dependencies — pulled in addition to requirements.txt.
-r requirements.txt

pytest>=8.0
pytest-cov>=4.1
ruff>=0.5

# Nuitka is only needed for producing redistributable binaries.
# Comment out if you only want to run from source.
nuitka>=2.0
patchelf>=0.17; platform_system == "Linux"
